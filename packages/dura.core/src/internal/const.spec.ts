import { __COMMIT__, ActionTypes } from './const';

it('should to be defined', () => {
  expect(__COMMIT__).toBeDefined();
});

it('should to be defined', () => {
  expect(ActionTypes).toBeDefined();
});
